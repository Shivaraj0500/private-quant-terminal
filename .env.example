# Upstox API credentials
UPSTOX_CLIENT_ID=your_upstox_client_id
UPSTOX_CLIENT_SECRET=your_upstox_client_secret

# This must exactly match the redirect URI configured
# in your Upstox developer application.
UPSTOX_REDIRECT_URI=http://localhost:8000/broker/upstox/callback
